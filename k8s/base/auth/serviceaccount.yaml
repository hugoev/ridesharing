# Auth ServiceAccount — least-privilege identity for the API auth pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth
  namespace: rideshare
  labels:
    app.kubernetes.io/name: auth
    app.kubernetes.io/component: api
