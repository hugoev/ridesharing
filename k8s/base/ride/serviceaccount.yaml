# Ride ServiceAccount — least-privilege identity for the API ride pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ride
  namespace: rideshare
  labels:
    app.kubernetes.io/name: ride
    app.kubernetes.io/component: api
