apiVersion: v1
kind: Service
metadata:
  name: ride
  namespace: rideshare
  labels:
    app.kubernetes.io/name: ride
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ride
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
