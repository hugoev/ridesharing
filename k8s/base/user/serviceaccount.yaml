# User ServiceAccount — least-privilege identity for the API user pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: user
  namespace: rideshare
  labels:
    app.kubernetes.io/name: user
    app.kubernetes.io/component: api
