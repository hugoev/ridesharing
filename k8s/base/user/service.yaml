apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: rideshare
  labels:
    app.kubernetes.io/name: user
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
