apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: rideshare
  labels:
    app.kubernetes.io/name: payment
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payment
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
