# Payment ServiceAccount — least-privilege identity for the API payment pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment
  namespace: rideshare
  labels:
    app.kubernetes.io/name: payment
    app.kubernetes.io/component: api
