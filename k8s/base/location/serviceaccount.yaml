# Location ServiceAccount — least-privilege identity for the API location pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: location
  namespace: rideshare
  labels:
    app.kubernetes.io/name: location
    app.kubernetes.io/component: api
