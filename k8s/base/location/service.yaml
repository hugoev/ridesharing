apiVersion: v1
kind: Service
metadata:
  name: location
  namespace: rideshare
  labels:
    app.kubernetes.io/name: location
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: location
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
